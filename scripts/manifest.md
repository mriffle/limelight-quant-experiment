---
schema_version: 1
generated: 2026-09-22
---

# Script registry

| Task | Path | Kind | Status | Provides | Uses | Seeded from | Description |
|------|------|------|--------|----------|------|-------------|-------------|
| metadata-characterize | scripts/promoted/metadata_characterize.py | analysis | promoted | — | common.hashing, common.design | batch-correct-combat@0.2 | Stage-1 metadata validity checks, design-invariant hypothesis tests (batch/run-order/pairing), confounding statistics (Cramer's V raw + bias-corrected), and cohort-characterization tables for the raloxifene/HLM LFQ metadata. Reads header lines only; makes no figures. |
| metadata-figures | scripts/promoted/metadata_figures.py | analysis | promoted | — | common.hashing, common.design, common.figures.colors, common.figures.figure_io | — | Stage-1 cohort figures (level counts, condition x batch, condition x run half, within-batch run layout) rendered from the precomputed results/metadata tables; registry colors, dual export + separate legend, per-figure provenance JSON. Refuses to render if the upstream run failed (FAILED.json present). |
| — | scripts/promoted/common/hashing.py | module | promoted | sha256_of_file, compute_file_hashes, compute_data_version, duplicates | — | — | sha256 file hashing + role-keyed data-version stamp, shared by metadata_characterize.py and metadata_figures.py so there is exactly one hashing implementation in the project. |
| — | scripts/promoted/common/design.py | module | promoted | run_half_label, FAILURE_MARKER_NAME | — | — | The single run-half (ceil(n/2), middle sample -> early) rule and the upstream-failure-marker filename shared by metadata_characterize.py and metadata_figures.py. |
| — | scripts/promoted/common/figures/colors.py | module | promoted | BACKGROUND_COLOR, Palette, CategoricalPaletteExceededError, DEFAULT_REGISTRY_PATH, load_registry, load_palette, assign_colors, get_color | — | okabe-ito-colors@0.2 | Project color registry: reads/extends state/color_registry.json so a given (category, value) gets the same Okabe-Ito color in every figure. Deterministic next-unused-color assignment, persisted; gray background labels outside the palette; raises CategoricalPaletteExceededError past 8 categories (the >8-category guard). Study-agnostic; fail-loud. |
| — | scripts/promoted/common/figures/figure_io.py | module | promoted | PUBLICATION_RCPARAMS, FigureArtifacts, publication_style, save_figure | — | figure-io@0.3 | Figure save helpers enforcing the visualization conventions: dual export (SVG vector + 300-DPI PNG) plus an optional companion legend figure exported as <base>.legend.{svg,png} (kept out of the plot so it cannot overlap the data), and a shared publication matplotlib style. Deviation from figure-io@0.3: SVG output is byte-deterministic (fixed svg.hashsalt, no Date metadata), and save_figure must be called inside publication_style(). Study-agnostic; fail-loud. |
| — | scripts/promoted/loaders/data_loading.py | module | promoted | Dataset, Scale, LOG_SCALES | — | wide-data-loader@0.5 | The Dataset contract (abundances/feature_names/feature_metadata/metadata/scale) carried over from the wide-data-loader template, trimmed of the generic id_columns loaders (load_wide_data/load_precursor_data) that no project script calls -- this project's files need custom loaders instead (protein_loader.py/peptide_loader.py/limelight_loader.py). |
| — | scripts/promoted/loaders/normalize.py | module | promoted | NormalizationMethod, normalize, log2_transform, to_linear, median_center | loaders.data_loading | normalize@0.4 | Verified Dataset normalization (median/mad/vsn), log2 transform, plain-log->linear inversion, log-domain median centering; scale-tag guarded against double-logging. Unchanged from the lib template except the loaders.data_loading import path. |
| — | scripts/promoted/loaders/batch_correct.py | module | promoted | BatchConfoundingWarning, BatchPassthroughWarning, ConfoundingReport, assess_batch_confounding, combat_correct | loaders.data_loading | batch-correct-combat@0.2 | Verified ComBat batch correction on a Dataset, batch-label-only (no covariate preserved); assess_batch_confounding (bias-corrected Cramer's V). Unchanged from the lib template except the loaders.data_loading import path. Requires pycombat. |
| — | scripts/promoted/loaders/dataset_io.py | module | promoted | save_dataset, load_dataset | loaders.data_loading | dataset-io@0.1 | Faithful on-disk round-trip for the Dataset contract (abundances.npy + parquet + dataset.json sidecar), re-verifying shape/pairing/scale on load. Unchanged from the lib template except the loaders.data_loading import path. |
| — | scripts/promoted/loaders/missing_values.py | module | promoted | ImputeMethod, MissingValueScaleError, MissingValueResult, handle_missing | loaders.data_loading | missing-values@0.1 | Verified Dataset missing-value handling: optional max-missing-fraction feature filter, then optional zero/mean/median/KNN imputation. This project uses only the filter half (max_missing_fraction=0, impute=None) for the complete-non-contaminant-feature QC state. Unchanged from the lib template except the loaders.data_loading import path. |
| — | scripts/promoted/loaders/samples.py | module | promoted | read_samples, REQUIRED_SAMPLE_COLUMNS | — | — | Reads + validates results/metadata/samples.tsv (required columns present, sample_id/search_scan_file_id unique) and returns it in acquisition order (batch, run_position_within_batch), re-derived rather than trusted from file row order. Shared by protein_loader.py/peptide_loader.py/limelight_loader.py. |
| — | scripts/promoted/loaders/text_io.py | module | promoted | read_raw_header, assert_no_duplicate_headers | — | — | Reads a wide data file's raw first-line header and asserts no column name repeats, before pandas can silently rename a duplicate column (dropping its data with no error). Shared by protein_loader.py/peptide_loader.py/limelight_loader.py. |
| — | scripts/promoted/loaders/protein_loader.py | module | promoted | ProteinLoadResult, load_protein_dataset, PROTEIN_ID_RE, CONTAMINANT_RE, PSVID_PREFIX_RE, refine_contaminants, parse_abundance_matrix | loaders.data_loading, loaders.samples, loaders.text_io | wide-data-loader@0.5 | Verified loader for data/protein-quants.tsv: feature id = Protein Groups (text); sample columns paired 1:1 to samples.tsv (fail loud on orphan/duplicate); both '0' and literal 'NaN' raw tokens -> NaN, with a per-cell boolean token mask kept distinguishable (was_nan_token/was_zero_token); feature_metadata parsed into accession/entry/psvid plus is_contaminant/contaminant_grouped_with_real. is_contaminant is the scientist-confirmed dump-refined rule (refine_contaminants; 33 true contaminants of the 52 no-accession candidates -- the other 19 are the first member of a Limelight group with a real-accession sibling and are kept) when protein_limelight_file is given, else the coarser no-accession-form-only flag. Custom loader (not the generic wide-data-loader), per the template's own guidance for files that don't fit its shape. |
| — | scripts/promoted/loaders/peptide_loader.py | module | promoted | PeptideLoadResult, load_peptide_dataset, PEPTIDE_ID_RE, POSITIVE_DETECTION_TYPES, ZERO_DETECTION_TYPES | loaders.data_loading, loaders.protein_loader, loaders.samples, loaders.text_io | wide-data-loader@0.5 | Verified loader for data/peptide-quants.tsv: feature id = Sequence (base sequence + trailing total-mod-mass bracket, cross-checked against Base Sequence); '0' -> NaN; feature_metadata carries base_sequence/protein_groups/is_contaminant/total_mod_mass; also returns the per-cell Detection Type matrix and asserts the Stage-2 intensity<->Detection Type invariant. is_contaminant uses the scientist-confirmed dump-refined 33-id contaminant set (any ';'-member in contaminant_ids, from protein_loader.refine_contaminants) when given, else the coarser no-accession-form-only regex match (449 vs. 675 peptides). Reuses protein_loader's token-aware parser and contaminant regex. |
| — | scripts/promoted/loaders/limelight_loader.py | module | promoted | LimelightProteinCounts, load_limelight_protein_counts, sig3 | loaders.protein_loader, loaders.samples, loaders.text_io | — | Verified loader for data/protein-limelight-table-dump.txt: PSMs (thousands-separator int; blank -> 0) and NSAF (3 sig figs; blank -> 0) per sample x protein-group. Per-run column labels resolved to samples by VALUE CORRESPONDENCE against sig3-rounded protein-quants intensities (>= 0.999 best match / <= 0.9 runner-up), cross-checked against results/stage2/limelight_label_map.tsv. Handles the dump's literal FlashLFQ "not quantifiable across runs" token. 27 comma-joined indistinguishable-group rows keep the group string + an explicit member-accession-key list; the dump<->protein-quants relation is 1:1 via each row's first member (verified bijection over all 4,344 rows; the 27 groups' 28 other members appear in no protein-quants row -- corrects an earlier, wrong "many-to-one"/"quants split rows" description). Returns a dedicated tidy container, not the Dataset contract (see the module docstring for why, despite the 1:1 relation). |
| verify-loaders | scripts/promoted/verify_loaders.py | analysis | promoted | — | common.hashing, common.design, loaders.protein_loader, loaders.peptide_loader, loaders.limelight_loader, loaders.samples | — | Stage-3 integrity-gate verification (obligation B): loads the real protein/peptide/Limelight data through the project loaders and cross-checks raw row/column counts, seeded random spot-cell reconciliation (>=200 cells/matrix) against an INDEPENDENT csv-module parse, orientation, dtypes, value ranges, identifier round-trip, missing-token counts, the peptide Detection-Type invariant (independently re-tallied), dump-refined contaminant/complete-feature counts (33/449; 1,801/10,229), exact sample<->metadata pairing INDEPENDENTLY RE-DERIVED row-by-row from data/metadata.tsv + data/scan-file-search-id-mapping.txt (never samples.tsv), the Limelight label map, a FULL (non-sampled) PSMs/NSAF reconciliation of every one of the 4,344x8 cells (label->sample via the cross-check file, also read with csv), a two-way total/median derivation, and that the combined data_version over the same six roles as results/metadata/data_version.json reproduces that file's stamp exactly. Fail-loud (FAILED marker + nonzero exit) on any mismatch; writes results/stage3/verification.{json,md}. |
| qc-prep | scripts/promoted/qc_prep.py | analysis | promoted | — | common.hashing, common.design, loaders.protein_loader, loaders.peptide_loader, loaders.limelight_loader, loaders.samples, loaders.missing_values, loaders.normalize, loaders.batch_correct, loaders.dataset_io, loaders.data_loading | — | Stage-3 QC prep-once: materializes raw_linear / raw_linear_complete / raw_log / normalized_linear / normalized_log / batch_corrected_log / batch_corrected_linear Dataset states for protein and peptide levels, plus an nsaf level (protein-group NSAF from load_limelight_protein_counts: raw_linear / raw_linear_complete / raw_log only, no normalize/ComBat -- NSAF is already normalized) and a psm level (PSM counts as-is: raw_linear / raw_linear_complete only, no normalize/ComBat/log2 -- scientist's explicit choice; its raw_linear_complete feature set is asserted identical to the nsaf level's), under results/qc_states/<level>/<state>/ via dataset_io.save_dataset. is_contaminant uses the scientist-confirmed dump-refined rule (33 protein / 449 peptide) at every level. Also saves the PSM reference matrix (results/qc_states/nsaf/psms.tsv), runs assess_batch_confounding once (batch x condition, batch x candidate_pair) into results/stage3/batch_confounding.json, records normalization params + feature counts + data_version in results/qc_states/manifest.json, and asserts the combined data_version over the same six roles as results/metadata/data_version.json reproduces that file's stamp exactly. Batch correction is batch-label-only; ComBat failure/warnings are recorded, never silently worked around; fails loud (FAILED marker, cleared at the start of every run) on any crash. |
| — | scripts/promoted/qc_figures/id_depth.py | module | promoted | — | common.figures, loaders.data_loading | id-depth@0.1 | Identification-depth bar charts per run (Stage-3 QC; adapted from lib/figures). |
| — | scripts/promoted/qc_figures/missingness.py | module | promoted | — | common.figures, loaders.data_loading | missingness@0.1 | Completeness curve + MNAR diagnostic (Stage-3 QC; adapted from lib/figures). |
| — | scripts/promoted/qc_figures/dynamic_range.py | module | promoted | — | common.figures, loaders.data_loading | dynamic-range@0.1 | Rank-abundance / dynamic-range curve (Stage-3 QC; adapted from lib/figures). |
| — | scripts/promoted/qc_figures/abundance_boxplot.py | module | promoted | — | common.figures, loaders.data_loading | abundance-boxplot@0.1 | Per-sample abundance box plots across processing states (Stage-3 QC; adapted from lib/figures). |
| — | scripts/promoted/qc_figures/cv.py | module | promoted | — | common.figures, loaders.data_loading | cv-plot@0.1 | Per-feature CV distributions across processing states (Stage-3 QC; adapted from lib/figures). |
| — | scripts/promoted/qc_figures/correlation.py | module | promoted | — | common.figures, loaders.data_loading | sample-correlation@0.1 | Clustered sample-correlation heatmap (Stage-3 QC; adapted from lib/figures). |
| — | scripts/promoted/qc_figures/pca.py | module | promoted | — | common.figures, loaders.data_loading | pca-plot@0.3 | PCA small multiples across processing states (Stage-3 QC; adapted from lib/figures). |
| qc-fig-id-depth | scripts/promoted/qc_fig_id_depth.py | analysis | promoted | — | qc_figures, loaders.dataset_io, common.figures | — | Stage-3 QC runner: renders the id-depth family from results/qc_states (intensity; protein + peptide). |
| qc-fig-missingness | scripts/promoted/qc_fig_missingness.py | analysis | promoted | — | qc_figures, loaders.dataset_io, common.figures | — | Stage-3 QC runner: renders the missingness family from results/qc_states (intensity; protein + peptide). |
| qc-fig-dynamic-range | scripts/promoted/qc_fig_dynamic_range.py | analysis | promoted | — | qc_figures, loaders.dataset_io, common.figures | — | Stage-3 QC runner: renders the dynamic-range family from results/qc_states (intensity; protein + peptide). |
| qc-fig-abundance-boxplot | scripts/promoted/qc_fig_abundance_boxplot.py | analysis | promoted | — | qc_figures, loaders.dataset_io, common.figures | — | Stage-3 QC runner: renders the abundance-boxplot family from results/qc_states (intensity; protein + peptide). |
| qc-fig-cv | scripts/promoted/qc_fig_cv.py | analysis | promoted | — | qc_figures, loaders.dataset_io, common.figures | — | Stage-3 QC runner: renders the cv family from results/qc_states (intensity; protein + peptide). |
| qc-fig-sample-correlation | scripts/promoted/qc_fig_sample_correlation.py | analysis | promoted | — | qc_figures, loaders.dataset_io, common.figures | — | Stage-3 QC runner: renders the sample-correlation family from results/qc_states (intensity; protein + peptide). |
| qc-fig-pca | scripts/promoted/qc_fig_pca.py | analysis | promoted | — | qc_figures, loaders.dataset_io, common.figures | — | Stage-3 QC runner: renders the pca family from results/qc_states (intensity; protein + peptide). |
| qc-fig-spectral | scripts/promoted/qc_fig_spectral.py | analysis | promoted | — | qc_figures, loaders.dataset_io, common.figures | — | Stage-3 QC runner: NSAF (as-is) and raw PSM-count versions of all seven QC families. |
